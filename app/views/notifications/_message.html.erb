<%= link_to channel_id_path(notification.notifiable.id), class: "dropdown-item #{"unread" if !notification.read_at?}" do %>
   <%= notification.actor.username %>
   <%= notification.action %>
   in <%= notification.notifiable.name %>
   <br>
 <% end %>